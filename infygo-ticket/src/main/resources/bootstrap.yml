server:
  port: 9500
spring:
  application:
    name: infygo-ticket
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        data-key: data
        enabled: true
        fail-fast: false
        prefix: config
        profile-separator: '::'
        default-context: application
        format: YAML