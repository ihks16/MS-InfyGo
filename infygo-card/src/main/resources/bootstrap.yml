server:
  port: 9100
spring:
  application:
    name: infygo-card
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        data-key: data
        enabled: true
        fail-fast: false
        prefix: config
        profile-separator: '::'
        default-context: application
        format: YAML